<script>

  const gallery = [<% @works.each do |work| %>
    '<a href="/users/<%= work.user_id %>"><img src="<%= work.artwork_image %>"alt="<%= work.title %>"></a>', <% end %>];
</script>

<div class="works-explore-container">
  <h1>All Works</h1>
  <div id="gallery-container">

    <div id="photos"></div>
  </div>


<div class="button-explore-container">
<%= button_to 'Get started', login_path, :class => 'btn-explore first', :id => 'get-started', :method => 'get' %>
</div>
</div>
