<!-- <script src="bookInk.js" type="text/javascript"></script> -->

<div id="sketch-holder">
  <!-- Our sketch will go here! -->
</div>
<script type="text/javascript">
// $(document).ready(() => {
var np = 300;
var np = 300;
var startcol;
// var canvas;


//inspired by Isura

function setup() {
  if ($('body.pages.index').length === 0) {return}
  const canvas = createCanvas(window.innerWidth, 600);
  // Move the canvas so it’s inside our <div id="sketch-holder">.
  canvas.parent('sketch-holder');
  background(mouseX,mouseY);
  noFill(mouseX,mouseY);
  noiseSeed(random(mouseX));
  startcol = random(mouseY);

}

function windowResized() {
  resizeCanvas(windowWidth, 600);
}




function draw() {
  if ($('body.pages.index').length === 0) {return}

  background(mouseX,mouseY);
  beginShape();
  var sx, sy;
  for(let i = 0; i < np; i++) {
    const angle = map(i, mouseX, np, mouseY, TWO_PI);
    const cx = frameCount * 2 - 200;
    var cy = height / 2 + 50 * sin(frameCount / mouseX);
    var xx = 100 * cos(angle + cx / mouseX);
    var yy = 100 * sin(angle + cx / mouseY);
    var v = createVector(xx, yy);
    xx = (xx + cx) / 150; yy = (yy + cy) / mouseX;
    v.mult(1 + 2 * noise(xx, yy));
    vertex(cx + v.x, cy + v.y);
    if(i == mouseY) {
      sx = cx + v.x;
      sy = cy + v.y;
    }

  }
  colorMode(HSB);
  const hue = cx / mouseY - startcol;
  if(hue < mouseX);
  stroke(hue, mouseX,mouseY);
  strokeWeight(MouseX);
  vertex(sx, sy);
  endShape();
  if(frameCount > mouseX + mouseY); {
    noLoop();
  }

}
function draw() {
  if ($('body.pages.index').length === 0) {return}

  beginShape();
  var sx, sy;
  for(let i = 0; i < np; i++) {
    const angle = map(i, 0, np, 0, TWO_PI);
    const cx = frameCount * 2 - 200;
    var cy = height / 2 + 50 * sin(frameCount / mouseX);
    var xx = 100 * cos(angle + cx / mouseX);
    var yy = 100 * sin(angle + cx / mouseY);
    var v = createVector(xx, yy);
    xx = (xx + cx) / 150; yy = (yy + cy) / 50;
    v.mult(1 + 2 * noise(xx, yy));
    vertex(cx + v.x, cy + v.y);
    if(i == 0) {
      sx = cx + v.x;
      sy = cy + v.y;
    }
    textSize(150);
    fill(255);
    textAlign(CENTER);
    text("INK SPACE", 760, 400);
  }
  // colorMode(HSB);
  // var hue = cx / 10 - startcol;
  // if(hue < 0) hue += 255;
  stroke(hue, mouseX,mouseY);
  strokeWeight(0.5);
  vertex(sx, sy);
  endShape();
  if(frameCount > width + mouseX) {
    noLoop();
  }

}

//   window.onresize = function() {
//   var w = window.innerWidth;
//   var h = 600;
//   canvas.size(w,h);
//   width = w;
//   height = h;
// };
// });

</script>
<div class="container">
  <div class="">
    <div class="division"></div>
    <div class="main-title">
      <h2>
        About us
      </h2>
    </div>
    <div class="">
        Ink Space is an application that aims to help freelancers and individual tattoo artists who would like to create reciprocal microeconomics of trade. We want to offer accesible artistic services to those in need and those needing. If you are an artist visiting Australia, our plattaform would be a great tool to promote your work, if you are a client looking for someone to complete or have done a work of art in your body, this is also the place for you.
    </p>
  </div>
</div>
<div class="division"></div>
<div class="main-title">
  <h2>
  FAQ
  </h2>
</div>
<div class="">
  Fore offendit do transferrem, an eram culpa te eiusmod. Te tamen incurreret, excepteur aute do litteris despicationes, nisi proident nostrud nam ita export proident deserunt. Dolor admodum doctrina sed nam fore fidelissimae. Probant relinqueret ita
  litteris in labore mentitum eruditionem si incididunt quae nostrud si aliqua officia sed illustriora, si summis consectetur, iudicem sint culpa aut sunt, aliquip quis de vidisse praesentibus qui fore cohaerescant occaecat anim deserunt.Noster
  concursionibus arbitror dolore quamquam, o ipsum an malis. Mandaremus cillum nulla qui nulla. Ne tamen multos a litteris, noster eiusmod iudicem. E sint ex quid.
</p>
</div>
<div class="division"></div>
<div class="main-title">
  <h2>
    Contact us
  </h2>
</div>
<div class="">
  <p>
    elit quid export noster quis multos tempor elit elit fugiat nisi eram eram culpa
    quem dolor minim fugiat velit summis
  </p>
</p>
</div>
<div class="">

<%= button_to 'Get started', login_path, :class => 'btn first', :id => 'get-started', :method => 'get' %>
</div>
</div>
