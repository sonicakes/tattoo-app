<div id="sketch-holder">
  <!-- Our sketch will go here! -->
</div>
<script type="text/javascript">
let cnv;
  // code for only somecontroller.someaction here!
function setup() {
  cnv = createCanvas(600, 600);
  const x = (windowWidth - width) / 2;
  const y = (windowHeight - height) / 2;
  cnv.parent('sketch-holder');
  console.log('position', x, y)
  if (cnv.position) {
    cnv.position(x, y);
  }
  resetDrawing();
  const button = createButton("reset").style("border", "2px solid black");
  button.position(960, 100);
  button.mousePressed(resetDrawing);
  const save = createButton("save").style("border", "2px solid black");
  save.position(960, 130);
  save.mousePressed(saveTattoo);

}

 function resetDrawing() {
  background (0);
  strokeWeight(3);
  stroke(255);
  redraw();
 }


function saveTattoo() {
 save('myTattoo.jpg');

}

function touchMoved() {
  line(mouseX, mouseY, pmouseX, pmouseY);
  return false;
}
</script>
